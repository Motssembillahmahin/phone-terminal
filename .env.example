# Server configuration
# Copy this file to .env and fill in your values

# Required for remote access. Generate one: python3 gen-token.py
AUTH_TOKEN=

# Optional: port (default 3000)
# PORT=3000

# Optional: shell binary (default bash)
# SHELL=/bin/zsh
